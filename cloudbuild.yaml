steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/github.com/slyberia/portfolio2.0:$COMMIT_SHA
      - -f
      - Dockerfile
      - .

images:
  - gcr.io/$PROJECT_ID/github.com/slyberia/portfolio2.0:$COMMIT_SHA

options:
  logging: CLOUD_LOGGING_ONLY
