Phase 3B — Track Configurations
Goal: Map existing portfolio tracks and projects to the newly established Role Lanes.
Scope:

1. Locate the configuration files for the portfolio tracks (e.g., `src/data/`, `src/constants.ts`, or wherever project data is stored).
2. Apply the canonical `roleLanes` (defined in 3A: "Implementation / CSE-lite", "Ops Analytics / QA", "GIS / Spatial Systems", "AI Workflow / Portfolio Governance") to the appropriate projects.
   Strict Constraints: Do not modify UI components, CSS, or routing logic. Only update the data payloads.
